if(TARGET galay::c-common AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/common/CMakeLists.txt")
    add_subdirectory(common)
endif()

if(TARGET galay::c-utils AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/utils/CMakeLists.txt")
    add_subdirectory(utils)
endif()
if(TARGET galay::c-kernel AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/kernel/CMakeLists.txt")
    add_subdirectory(kernel)
endif()
if(TARGET galay::c-ssl AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/ssl/CMakeLists.txt")
    add_subdirectory(ssl)
endif()
if(TARGET galay::c-http AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/http/CMakeLists.txt")
    add_subdirectory(http)
endif()
if(TARGET galay::c-ws AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/ws/CMakeLists.txt")
    add_subdirectory(ws)
endif()
if(TARGET galay::c-http2 AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/http2/CMakeLists.txt")
    add_subdirectory(http2)
endif()
if(TARGET galay::c-redis AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/redis/CMakeLists.txt")
    add_subdirectory(redis)
endif()
if(TARGET galay::c-rpc AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/rpc/CMakeLists.txt")
    add_subdirectory(rpc)
endif()
if(TARGET galay::c-mysql AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/mysql/CMakeLists.txt")
    add_subdirectory(mysql)
endif()
if(TARGET galay::c-mongo AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/mongo/CMakeLists.txt")
    add_subdirectory(mongo)
endif()
if(TARGET galay::c-etcd AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/etcd/CMakeLists.txt")
    add_subdirectory(etcd)
endif()
if(TARGET galay::c-mcp AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/mcp/CMakeLists.txt")
    add_subdirectory(mcp)
endif()
if(TARGET galay::c-tracing AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tracing/CMakeLists.txt")
    add_subdirectory(tracing)
endif()
