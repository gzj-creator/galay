# 工程准则

用于减少常见的设计与实现错误；项目中的更具体约定优先。

## 1. 先澄清，再动手

- 说明关键假设；需求、边界或取舍不清时先提问，不要默默猜测。
- 多步骤任务先给出可验证的简要计划；以测试、构建或明确检查项作为完成标准。
- 修改前先阅读相关代码、现有依赖和成熟产品的已验证做法；不要从零发明已有方案。

## 2. 最小、渐进、可维护

- 选择满足当前需求的最简单实现：不做预防性抽象、不加多余配置或一次性 helper。
- 先跑通最小端到端版本，再逐步增加分层和复杂度；不因尚未完成的复杂设计拆掉能工作的实现。
- 组件保持模块化与关注点分离；仅在能显著降低复杂度或符合既有模块边界时再拆分。
- 架构决策应可长期维护，拒绝明知以后必须替换的临时方案；但不要为未来假设提前实现。

## 3. 依赖与兼容性

- 先确认项目现有依赖能否解决问题，再考虑新增依赖或自行实现。
- 优先使用成熟、持续维护的库；自行实现必须有明确理由。
- 不保留向后兼容：删除过时代码，不添加兼容层、迁移逻辑或 fallback。

## 4. 精准修改与验证

- 只修改需求直接涉及的代码，保持现有风格；不顺手重构、格式化或删除既有无关死代码。
- 清理由本次修改产生的未使用代码；每一行改动都应能追溯到需求。
- 修复缺陷先写能复现问题的测试；新增行为写对应测试；重构前后测试必须通过。

## 5. 显式错误与结果处理

- 不用异常隐藏或控制可恢复错误。C++ 生产代码不得新增 `throw`、`try`、`catch`；可恢复错误用 `std::expected` 显式返回。
- C API 用结果结构体或明确错误码表达失败；每个公开错误码枚举提供覆盖全部枚举值的 `*_get_error(...)`。
- C/C++ 边界立即转换 errno、系统错误或 typed error，保留失败原因。
- 所有非 `void` 返回值必须使用、检查或显式向上传播；清理和回滚返回值同样必须处理。
- 仅在接口明确允许时才能忽略返回值，并用简短注释说明原因。
