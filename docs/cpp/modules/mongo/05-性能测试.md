# 05-性能测试

## 1. 测试目标

- 验证 `galay-mongo` 在真实 MongoDB 服务上的连通性与稳定性。
- 对比 sync/async 在相同 `ping` 负载下的吞吐与延迟。
- 输出可复现实验命令，作为后续回归基线。

## 2. 测试时间与环境

- 测试日期：2026-02-15
- 客户端系统：Darwin Kernel 24.6.0 (arm64)
- 构建模式：Release
- MongoDB 地址：`140.143.142.251:27017`
- 数据库：`admin`
- 鉴权：未提供用户名密码（服务端当前未启用鉴权）

## 3. 测试程序

- `b1_ping`：同步会话 + 多线程并发
- `b2_ping`：异步客户端 + 协程并发

构建：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DGALAY_MONGO_BUILD_BENCHMARKS=ON
cmake --build build --parallel
```

执行命令：

```bash
./build/benchmark/b1_ping 3000 10 140.143.142.251 27017 admin
./build/benchmark/b1_ping 5000 50 140.143.142.251 27017 admin
./build/benchmark/b1_ping 8000 100 140.143.142.251 27017 admin

./build/benchmark/b2_ping 3000 10 140.143.142.251 27017 admin
./build/benchmark/b2_ping 5000 50 140.143.142.251 27017 admin
./build/benchmark/b2_ping 8000 100 140.143.142.251 27017 admin
```

## 4. 测试结果

### 4.1 Sync (`b1_ping`)

| total | concurrency | Success | Errors | Duration(ms) | Requests/sec | p50(ms) | p95(ms) | p99(ms) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 3000 | 10 | 3000 | 0 | 9171 | 327.118 | 30.2662 | 34.9130 | 35.9737 |
| 5000 | 50 | 5000 | 0 | 3183 | 1570.850 | 31.6150 | 35.5040 | 36.6973 |
| 8000 | 100 | 8000 | 0 | 2618 | 3055.770 | 32.6711 | 36.3128 | 38.4885 |

### 4.2 Async (`b2_ping`)

| total | concurrency | Success | Errors | Duration(ms) | Requests/sec | p50(ms) | p95(ms) | p99(ms) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 3000 | 10 | 3000 | 0 | 9674 | 310.110 | 31.7652 | 34.3525 | 35.9416 |
| 5000 | 50 | 5000 | 0 | 3280 | 1524.390 | 31.2850 | 34.7462 | 36.7312 |
| 8000 | 100 | 8000 | 0 | 2807 | 2850.020 | 31.9926 | 35.8539 | 37.5235 |

### 4.3 对比摘要

| 并发 | Sync RPS | Async RPS | RPS 差值 (Async-Sync) |
|---:|---:|---:|---:|
| 10 | 327.118 | 310.110 | -17.008 |
| 50 | 1570.850 | 1524.390 | -46.460 |
| 100 | 3055.770 | 2850.020 | -205.750 |

结论：

- 两种实现都在 6 组用例中实现 `Errors=0`。
- 延迟分位接近，p50/p95/p99 都在同一数量级。
- 该轮测试下 sync 吞吐略高于 async；主要原因是 async 版本每协程连接的调度与日志开销更明显。

## 5. 连通性验证（Example）

同一环境下 example 验证：

- `e1_ping_include`：`Mongo ping OK`
- `e2_ping_include`：`Async Mongo ping OK`

说明核心链路可用：`connect -> hello -> (可选 SCRAM) -> command(ping)`。

## 6. 结果解读与后续优化

建议后续压测继续补充：

- 认证开启场景（`SCRAM-SHA-256`）对吞吐影响。
- 更真实命令集（`find/insert/update/delete`）而不只 `ping`。
- async 压测时降低日志等级，减少连接成功日志对输出与开销的影响。

## 7. 优化补充（2026-02-16）

本轮优化项：

- 异步/同步连接启用 `TCP_NODELAY`。
- benchmark 热路径减少原子操作开销（每 worker 本地累计后合并）。
- `b2` 新增 `--fanout=N`（或 `GALAY_MONGO_BENCH_ASYNC_FANOUT`），用于高 RTT 场景扩展有效连接数。

同环境同参数对比（`total=8000, concurrency=100`）：

| 模式 | fanout | Requests/sec | p50(ms) | p95(ms) | p99(ms) | Errors |
|---|---:|---:|---:|---:|---:|---:|
| Async | 1 | 2832.86 | 32.5001 | 35.8309 | 37.1864 | 0 |
| Async | 2 | 3484.32 | 33.6484 | 39.9428 | 275.76 | 0 |

结论：

- `fanout=2` 吞吐约提升 `22.98%`。
- 尾延迟（特别 p99）会显著上升，适合“追求吞吐优先”的压测场景。

## 8. 继续优化补充（2026-02-16，第二轮）

本轮新增优化项：

- `MongoCommandAwaitable` 支持复用 `arm()`（修复复用路径编译问题）。
- 新增 async `ping` 快路径：缓存同库 `ping` 请求模板，仅替换 `requestId`，避免重复 BSON 编码。
- `b2_ping` 热路径改为调用 `client.ping(...)`，减少压测程序自身构造开销。

复测命令（串行执行，避免互相干扰）：

```bash
./build/benchmark/b2_ping 8000 100 140.143.142.251 27017 admin
./build/benchmark/b2_ping 8000 100 140.143.142.251 27017 admin --fanout=2
```

结果：

| 模式 | fanout | Requests/sec | p50(ms) | p95(ms) | p99(ms) | Errors |
|---|---:|---:|---:|---:|---:|---:|
| Async | 1 | 2856.12 | 31.9413 | 36.0207 | 37.7531 | 0 |
| Async | 2 | 3379.81 | 34.0630 | 40.6976 | 277.4580 | 0 |

结论：

- `fanout=1` 吞吐与上一轮基线接近，保持稳定。
- `fanout=2` 仍能明显提升吞吐，但高分位延迟上升，需按场景取舍。
- 当前远端压测已更接近 RTT/服务端处理能力上限，后续若继续提升 QPS，建议引入“单连接多 in-flight 命令”模式。

## 9. 同步 Connection RingBuffer 改造回归（2026-02-16，第三轮）

本轮新增优化项：

- 同步 `protocol::Connection` 接收路径由线性 `vector + memmove` 改为 `RingBuffer + readv`。
- 新增大报文 bridge：当单条消息大于 ring 容量时，自动切换到“ring 已收数据 + socket 分段直读”。
- 保持原有错误语义（超时/断连/协议错误）不变。

回归命令：

```bash
./build/benchmark/b1_ping 8000 100 140.143.142.251 27017 admin
./build/benchmark/b2_ping 8000 100 140.143.142.251 27017 admin
```

结果：

| 程序 | total | concurrency | Success | Errors | Duration(ms) | Requests/sec | p50(ms) | p95(ms) | p99(ms) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| b1_ping | 8000 | 100 | 8000 | 0 | 5335 | 1499.53 | 62.6260 | 92.5460 | 180.1990 |
| b2_ping | 8000 | 100 | 8000 | 0 | 5702 | 1403.02 | 64.3875 | 90.7435 | 104.7210 |

说明：

- 本轮主要目标是验证改造后功能/稳定性，无错误返回（`Errors=0`）达成。
- 该组数据受远端实例当时负载和网络抖动影响较大，绝对值建议仅作同环境当时快照，不直接与历史峰值做结论性对比。

## 11. 压测计划执行状态（2026-07-04）

本节对应 `docs/benchmark_plan.md` 中 `2.7 galay-mongo` 以及第 4/7/8 节报告与归档要求。本轮根据现场证据只做构建、目标、工具链与可运行性确认；用户已要求停止任何长时间构建、压测或等待外部服务，因此未启动长压测，也未补构建缺失的 benchmark 二进制。

### 10.1 构建与目标状态

| 项目 | `build/` | `../build-release` |
|---|---|---|
| `CMAKE_BUILD_TYPE` | `Debug` | `Release` |
| `BUILD_TESTING` | `OFF` | `ON` |
| `GALAY_BUILD_BENCHMARKS` | `OFF` | `ON` |
| C/C++ 编译器 | `/usr/bin/cc`, `/usr/bin/c++` | `/usr/bin/cc`, `/usr/bin/c++` |
| Mongo benchmark target | 未列出 Mongo benchmark target | `benchmark_mongo_sync_ping_throughput`, `benchmark_mongo_async_ping_throughput`, `benchmark_mongo_expected_error_boundaries`, `benchmark_mongo_crc32c` |
| Mongo benchmark 二进制 | 未发现 | 未发现；`../build-release/benchmark/cpp/mongo/` 只有 CMake/CTest 元数据，直接执行 `benchmark_mongo_sync_ping_throughput --help` 返回 `No such file or directory` |

源码侧 `benchmark/cpp/mongo/` 当前只有 `b1_ping.cc`、`b2_ping.cc`、`b3_expected_error_boundaries.cc`、`b4_crc32c.cc`。未发现计划中的 `benchmark_mongo_client`、`benchmark_mongoc_client`，也未发现插入、`find/findOne/aggregate` 或连接池效率的专用 benchmark 源/目标。

### 10.2 MongoDB 与竞品可用性

| 实现/工具 | 版本 | 状态 | 证据 |
|---|---|---|---|
| MongoDB server (`127.0.0.1:27017`) | 未探测 | blocked | 用户要求停止等待外部服务；未执行 `nc`/`mongosh` 连接探测，不能推断服务可用 |
| `mongod` | 未取得 | unavailable | `command -v mongod` 返回退出码 1 |
| `mongosh` | 未取得 | unavailable | `command -v mongosh` 返回退出码 1 |
| `mongo` | 未取得 | unavailable | `command -v mongo` 返回退出码 1 |
| `mongoc` (C driver) | 未取得 | blocked | `pkg-config` 可用，但 `mongoc` 版本探测未在中断前完成 |
| `mongo-cxx-driver` | 未取得 | blocked | 版本探测未在中断前完成 |

### 10.3 计划场景状态

| 场景 | 计划要求 | 本轮状态 | 原因 |
|---|---|---|---|
| 文档插入 | 单文档、批量插入；1KB/10KB/100KB 文档 | blocked / not run | 当前未发现插入 benchmark 源、target 或 Release 二进制；本地 Mongo 服务安全可访问性未确认 |
| 查询性能 | `find`、`findOne`、`aggregate` | blocked / not run | 当前未发现查询 benchmark 源、target 或 Release 二进制；本地 Mongo 服务安全可访问性未确认 |
| 连接池效率 | 连接池大小与并发客户端压力 | blocked / not run | 当前未发现连接池 benchmark 源、target 或 Release 二进制；本地 Mongo 服务安全可访问性未确认 |
| 短 `ping` 冒烟 | 最多运行短测 | blocked / not run | Release Mongo ping target 已列出但二进制未落盘，未满足“已有 Release benchmark 二进制且 Mongo 服务可安全访问”的运行条件 |

### 10.4 结论

- 本轮没有产生新的 MongoDB 性能数据；历史第 4/7/8/9 节 ping 数据仍仅代表 2026-02-15 到 2026-02-16 的远端实例快照。
- 计划要求的插入、查询、连接池竞品对比尚未具备本地可运行条件，不能给出 galay 与 `mongoc` / `mongo-cxx-driver` 的性能优劣结论。
- 当前最明确的阻塞点是：Release 构建树启用了 benchmark 但 Mongo benchmark 二进制未生成；计划所需 insert/query/pool benchmark 目标不存在；MongoDB CLI 不可用；`127.0.0.1:27017` 未完成安全连通性确认。

### 10.5 后续补跑命令（未在本轮执行）

仅在确认不会影响已有数据、MongoDB 本地实例可安全访问且需要的二进制已生成后执行：

```bash
rtk cmake --build ../build-release --target benchmark_mongo_sync_ping_throughput benchmark_mongo_async_ping_throughput --parallel
rtk ../build-release/benchmark/cpp/mongo/benchmark_mongo_sync_ping_throughput --total 1 --concurrency 1 --timeout-sec 3 --host 127.0.0.1 --port 27017 --db admin
rtk ../build-release/benchmark/cpp/mongo/benchmark_mongo_async_ping_throughput --total 1 --concurrency 1 --timeout-sec 3 --host 127.0.0.1 --port 27017 --db admin
```

计划中的竞品/业务场景命令需要先补齐对应二进制后才能执行：

```bash
rtk ./benchmark_mongoc_client --uri mongodb://127.0.0.1:27017 --clients 50 --docs 10000 --doc-size 1024 --pool-size 10
rtk ./benchmark_mongo_client --uri mongodb://127.0.0.1:27017 --clients 50 --docs 10000 --doc-size 1024 --pool-size 10
```

原始状态归档：[`benchmark_data/2026-07-04_status.csv`](./benchmark_data/2026-07-04_status.csv)。
