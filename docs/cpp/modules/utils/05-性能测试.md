# 05-性能测试

本页描述当前仓库真实存在的 benchmark 资产、构建方式与结果口径。性能数字与机器、编译器、构建类型、系统负载强相关，不作为跨环境承诺。

## 1. 当前 benchmark 资产

| 项目 | 当前真实状态 |
|---|---|
| `benchmark/` 目录 | 存在，包含 LRU、ByteQueueView、RingBuffer、BloomFilter 与 CircuitBreaker benchmark |
| 顶层开关 | `BUILD_BENCHMARKS`，默认 `OFF` |
| CTest | benchmark 不注册为测试，避免默认验证变慢 |
| 当前 target | `lru_cache_benchmark`、`byte_queue_view_benchmark`、`ring_buffer_benchmark`、`bloom_filter_benchmark`、`circuit_breaker_benchmark` |

## 2. 构建命令

```bash
rtk cmake -S . -B cmake-build-bench -DCMAKE_BUILD_TYPE=Release -DBUILD_BENCHMARKS=ON -DBUILD_EXAMPLES=OFF -DBUILD_TESTING=OFF
rtk cmake --build cmake-build-bench
```

也可以只构建单个 target：

```bash
rtk cmake --build cmake-build-bench --target lru_cache_benchmark
rtk cmake --build cmake-build-bench --target byte_queue_view_benchmark
rtk cmake --build cmake-build-bench --target ring_buffer_benchmark
rtk cmake --build cmake-build-bench --target bloom_filter_benchmark
rtk cmake --build cmake-build-bench --target circuit_breaker_benchmark
```

## 3. 运行命令

```bash
rtk ./cmake-build-bench/benchmark/lru_cache_benchmark
rtk ./cmake-build-bench/benchmark/byte_queue_view_benchmark
rtk ./cmake-build-bench/benchmark/ring_buffer_benchmark
rtk ./cmake-build-bench/benchmark/bloom_filter_benchmark
rtk ./cmake-build-bench/benchmark/circuit_breaker_benchmark
```

## 4. 结果口径

- benchmark 源码会输出 workload、容量、吞吐和基本 checksum。
- `lru_cache_benchmark` 同时输出默认关闭统计的容量 LRU，以及显式 `EnableStats=true` 的统计开启版本。
- `byte_queue_view_benchmark` 覆盖追加/消费、增量压缩和长度前缀帧解析。
- `ring_buffer_benchmark` 覆盖拷贝写入/读取与环绕读写，POSIX 平台可通过单测覆盖 iovec 视图。
- `bloom_filter_benchmark` 覆盖 `addHash()`、命中查询和未命中查询，并输出观测到的假阳性数量。
- `circuit_breaker_benchmark` 覆盖 Closed 成功、Closed 失败、Open 拒绝、HalfOpen 探测成功，以及共享实例的多线程压测路径。
- 结果用于同一台机器上的相对比较，不作为发布版性能保证。
- 若要记录性能结论，应同时记录编译器、CPU、构建参数、样本次数和命令。

## 5. 当前已知性能相关限制

- `LruCache` 的纯容量模式避免时钟访问；运行统计默认关闭，只有 `LruCache<..., true>` 才累计计数；启用 TTL 后读写路径需要维护过期堆。
- `RingBuffer` 核心使用跨平台 span 视图；POSIX `iovec` 成员接口按平台宏保护，便于 kernel 侧直接迁移到 utils。
- `RandomLoadBalancer` / `WeightedRandomLoadBalancer` 使用共享 RNG；`RoundRobinLoadBalancer::append()` 也没有内部同步，共享实例的多线程修改仍需外部同步。
- `BlockingObjectPool::acquire()`、`ThreadPool::waitAll()`、`TaskWaiter::wait()` 是阻塞接口，不适合直接放进协程调度线程。

## 6. 下一步

- 想看真实示例来源： [04-示例代码](04-示例代码.md)
- 想看限制与线程安全边界： [06-高级主题](06-高级主题.md)
- 想看构建 FAQ： [07-常见问题](07-常见问题.md)

---

## 2026-07-03 压测计划归档状态

**测试环境**: 参考 [测试机器配置](../../../machine_config.md)  
**测试日期**: 2026-07-03  
**galay 版本**: `v4.0.1-2-gfd86efe-dirty` / commit `fd86efe`  
**本轮状态**: `blocked`，未运行正式 Release benchmark。

`docs/benchmark_plan.md` 的模块场景没有单独列出 `galay-utils` 竞品对比计划，但第 8 节的数据归档结构要求保留 `docs/cpp/modules/utils/05-性能测试.md` 与 `benchmark_data/`。本节只记录本轮状态，不补造竞品数据。

### 已发现 benchmark 目标

`build-release/` 已配置为 `CMAKE_BUILD_TYPE=Release` 且 `GALAY_BUILD_BENCHMARKS=ON`，目标清单包含：

| 目标 | 场景 | 本轮状态 |
|------|------|----------|
| `benchmark_utils_lru_cache_throughput` | LRU cache 吞吐 | not_run |
| `benchmark_utils_byte_queue_view_throughput` | ByteQueueView 吞吐 | not_run |
| `benchmark_utils_ring_buffer_throughput` | RingBuffer 吞吐 | not_run |
| `benchmark_utils_bloom_filter_throughput` | BloomFilter 吞吐 | not_run |
| `benchmark_utils_circuit_breaker_throughput` | CircuitBreaker 吞吐 | not_run |
| `benchmark_utils_resource_error_boundaries` | Resource/Error 边界 | not_run |
| `benchmark_utils_consistent_hash_lookup` | ConsistentHash lookup | not_run |

### 未运行原因

- `build/` 当前为 `Debug` 且 `GALAY_BUILD_BENCHMARKS=OFF`，不满足正式压测条件。
- `build-release/` 虽已具备目标清单，但本轮没有生成并运行 `benchmark_utils_*` 可执行文件。
- 压测计划没有指定 utils 的同类竞品，因此本轮不做第三方百分比对比。

### 原始数据

- [utils_benchmark_status_2026-07-03.csv](./benchmark_data/utils_benchmark_status_2026-07-03.csv)

### 补跑命令

```bash
rtk cmake --build ../build-release --target \
  benchmark_utils_lru_cache_throughput \
  benchmark_utils_byte_queue_view_throughput \
  benchmark_utils_ring_buffer_throughput \
  benchmark_utils_bloom_filter_throughput \
  benchmark_utils_circuit_breaker_throughput \
  benchmark_utils_resource_error_boundaries \
  benchmark_utils_consistent_hash_lookup \
  --parallel 4

rtk ../build-release/benchmark/cpp/utils/benchmark_utils_lru_cache_throughput
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_byte_queue_view_throughput
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_ring_buffer_throughput
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_bloom_filter_throughput
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_circuit_breaker_throughput
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_resource_error_boundaries
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_consistent_hash_lookup
```

## 2026-07-04 Release utils 压测结果

**测试环境**: 参考 [测试机器配置](../../../machine_config.md)  
**构建目录**: `build-release` (`CMAKE_BUILD_TYPE=Release`, `GALAY_BUILD_BENCHMARKS=ON`)  
**原始数据**: [utils_release_results_2026-07-04.csv](./benchmark_data/utils_release_results_2026-07-04.csv)

执行命令：

```bash
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_lru_cache_throughput
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_byte_queue_view_throughput
rtk ../build-release/benchmark/cpp/utils/benchmark_utils_ring_buffer_throughput
```

| 场景 | 指标 | 结果 |
|------|------|------|
| LruCache capacity read-heavy | 吞吐 | 173.99 Mops/s |
| LruCache capacity read-heavy | 延迟 | 5.75 ns/op |
| LruCache capacity mixed | 吞吐 | 19.27 Mops/s |
| LruCache capacity mixed | 延迟 | 51.90 ns/op |
| LruCache capacity write-heavy | 吞吐 | 13.29 Mops/s |
| ByteQueueView append+consume | 吞吐 | 31,538.61 MB/s |
| ByteQueueView incremental compact | 吞吐 | 14,439.63 MB/s |
| ByteQueueView framed parse | 吞吐 | 29,349.61 MB/s |
| RingBuffer write+consume | 吞吐 | 55,047.74 MB/s |
| RingBuffer write+read | 吞吐 | 30,858.57 MB/s |
| RingBuffer wrap-around | 吞吐 | 18,994.90 MB/s |

`utils` 不在本次服务端竞品压测主体内，本节作为内部 Release micro benchmark 归档。
