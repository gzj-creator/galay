# 05-性能测试

本页只归档本轮可审计证据：构建目录状态、benchmark target/二进制状态、竞品可用性探测、未运行原因和补跑命令。没有原始输出的数据不写入性能数字。

## 本轮状态

- 日期：2026-07-04
- 模块：`galay-rpc`
- 计划来源：`docs/benchmark_plan.md` 的 `2.9 galay-rpc`、第 4 节报告模板、第 7/8 节报告与归档要求。
- 允许写入范围：`docs/cpp/modules/rpc/05-性能测试.md`、`docs/cpp/modules/rpc/benchmark_data/**`、`docs/cpp/modules/rpc/configs/**`。
- 本轮未启动长压测，未构建新二进制，未等待外部服务。
- 原始状态 CSV：`docs/cpp/modules/rpc/benchmark_data/rpc_benchmark_status_2026-07-04.csv`
- 探测与补跑命令归档：`docs/cpp/modules/rpc/configs/rpc_benchmark_commands_2026-07-04.md`

## 测试环境摘要

| 项目 | 值 |
|------|----|
| OS | Darwin 25.3.0 arm64 |
| CPU | Apple M4 Pro |
| 逻辑 CPU | 12 |
| 内存 | 51539607552 bytes |
| C++ 编译器 | Apple clang 17.0.0 (`clang-1700.0.13.5`) |
| `protoc` | `libprotoc 29.3` |

本轮没有写入 `docs/machine_config.md`，因为任务限定写入范围不包含该文件。

## 构建目录状态

| 构建目录 | 生成器 | 构建类型 | `GALAY_BUILD_BENCHMARKS` | `BUILD_TESTING` | RPC benchmark target | RPC benchmark 二进制 | 状态 |
|----------|--------|----------|--------------------------|-----------------|----------------------|---------------------|------|
| `build/` | Ninja | Debug | OFF | OFF | 不在 Debug target help 中列出 | 未发现 `benchmark_rpc*` | 不适合压测 |
| `build-release/` | Unix Makefiles | Release | ON | ON | 已生成 `benchmark_rpc_*` targets | 未发现 `benchmark_rpc*` | 可构建但本轮未运行 |

`build-release` 中可见的 RPC benchmark targets：

- `benchmark_rpc_unary_rpc_throughput`
- `benchmark_rpc_streaming_rpc_throughput`
- `benchmark_rpc_service_discovery_latency`
- `benchmark_rpc_server_stream_throughput`
- `benchmark_rpc_bidirectional_stream_throughput`
- `benchmark_rpc_unary_loopback_latency`
- `benchmark_rpc_unary_mode_route_cache`
- `benchmark_rpc_stream_pressure`
- `benchmark_rpc_stream_loopback_latency`
- `benchmark_rpc_unary_concurrent_pressure`
- `benchmark_rpc_pool_pressure`
- `benchmark_rpc_managed_client_pressure`
- `benchmark_rpc_payload_scaling`
- `benchmark_rpc_connection_pool_endpoint_switching`

## 竞品版本与可用性

| 实现 | 探测命令 | 结果 | 状态 |
|------|----------|------|------|
| ghz | `which ghz`; `ghz --version` | 命令不存在 | unavailable |
| gRPC C++ | `which grpc_cpp_plugin`; `grpc_cpp_plugin --version`; `pkg-config --modversion grpc++`; `pkg-config --modversion grpc` | `grpc_cpp_plugin` 不存在，`pkg-config` 找不到 `grpc++`/`grpc` | unavailable |
| brpc | `which brpc`; `pkg-config --modversion brpc` | 命令不存在，`pkg-config` 找不到 `brpc` | unavailable |
| thrift | `which thrift`; `thrift --version` | 命令不存在 | unavailable |
| protobuf | `which protoc`; `protoc --version`; `brew list --versions protobuf` | `/opt/homebrew/bin/protoc`，`libprotoc 29.3`，Homebrew `protobuf 29.3` | available |

结论：当前环境没有可直接运行的 gRPC/ghz、brpc、thrift 对比基线，不能产出竞品性能数字。

## 一元调用

| 实现 | 场景 | 状态 | 说明 |
|------|------|------|------|
| galay-rpc | 一元 echo throughput | blocked | Release target `benchmark_rpc_unary_rpc_throughput` 存在，但 `build-release/benchmark/cpp/rpc/` 下没有已构建二进制。 |
| galay-rpc | 一元 loopback latency | blocked | Release target `benchmark_rpc_unary_loopback_latency` 存在；该场景无需外部服务，但本轮没有现成二进制，按要求未临时构建。 |
| galay-rpc | 一元并发压力 | blocked | Release target `benchmark_rpc_unary_concurrent_pressure` 存在；未发现二进制。 |
| gRPC/ghz | 一元 echo | unavailable | `ghz`、`grpc_cpp_plugin`、`grpc++`/`grpc` pkg-config 均不可用。 |
| brpc | 一元 echo | unavailable | 未发现 `brpc` 命令或 `brpc.pc`。 |
| thrift | 一元 echo | unavailable | 未发现 `thrift` 命令。 |

## 流式调用

| 实现 | 场景 | 状态 | 说明 |
|------|------|------|------|
| galay-rpc | `client_stream` / `server_stream` / `bidi` 模式吞吐 | blocked | Release target `benchmark_rpc_streaming_rpc_throughput` 存在，客户端支持 `-m unary|client_stream|server_stream|bidi`；未发现二进制。 |
| galay-rpc | server stream 服务端 | blocked | Release target `benchmark_rpc_server_stream_throughput` 存在；未发现二进制。 |
| galay-rpc | bidirectional stream throughput | blocked | Release target `benchmark_rpc_bidirectional_stream_throughput` 存在；未发现二进制。 |
| galay-rpc | stream loopback latency | blocked | Release target `benchmark_rpc_stream_loopback_latency` 存在；无需外部服务，但本轮没有现成二进制，按要求未临时构建。 |
| gRPC/brpc/thrift | 流式调用对比 | unavailable | 当前环境缺少对应工具链和可执行基线。 |

## 服务发现

| 实现 | 场景 | 状态 | 说明 |
|------|------|------|------|
| galay-rpc | 本地 `AsyncLocalServiceRegistry` 注册/发现/注销 | blocked | Release target `benchmark_rpc_service_discovery_latency` 存在；未发现二进制。该 benchmark 是本地内存注册中心，不代表 etcd/Consul 等外部注册中心。 |
| galay-rpc | etcd managed client pressure | unavailable | `benchmark_rpc_etcd_managed_client_pressure` 未出现在 `build-release` target help 中；需要启用对应 etcd 构建选项和外部 etcd 环境后单独验证。 |
| gRPC/brpc/thrift | 服务发现对比 | unavailable | 本轮未发现可运行的竞品服务发现 benchmark。 |

## 补跑命令

以下命令仅作为可复现补跑入口，本轮未执行构建或压测：

```bash
# 构建最小本地短测二进制
rtk cmake --build build-release --target \
  benchmark_rpc_unary_loopback_latency \
  benchmark_rpc_stream_loopback_latency \
  benchmark_rpc_service_discovery_latency

# 一元 loopback 短测
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_unary_loopback_latency 100

# 流式 loopback 短测
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_stream_loopback_latency -n 100 -s 128

# 本地服务发现短测
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_service_discovery_latency -w 4 -d 1 -i 1

# 一元/流式服务端+客户端吞吐短测
rtk cmake --build build-release --target \
  benchmark_rpc_unary_rpc_throughput \
  benchmark_rpc_streaming_rpc_throughput \
  benchmark_rpc_server_stream_throughput \
  benchmark_rpc_bidirectional_stream_throughput

rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_unary_rpc_throughput 9000 1 131072
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_streaming_rpc_throughput \
  -h 127.0.0.1 -p 9000 -c 4 -d 1 -s 128 -i 1 -l 1 -m unary
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_streaming_rpc_throughput \
  -h 127.0.0.1 -p 9000 -c 4 -d 1 -s 128 -i 1 -l 1 -m client_stream
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_streaming_rpc_throughput \
  -h 127.0.0.1 -p 9000 -c 4 -d 1 -s 128 -i 1 -l 1 -m server_stream
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_streaming_rpc_throughput \
  -h 127.0.0.1 -p 9000 -c 4 -d 1 -s 128 -i 1 -l 1 -m bidi

rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_server_stream_throughput 9100 1 131072
rtk ./build-release/benchmark/cpp/rpc/benchmark_rpc_bidirectional_stream_throughput \
  -h 127.0.0.1 -p 9100 -c 4 -d 1 -s 128 -f 4 -w 1 -i 1
```

服务端命令会持续运行，正式补跑时需要单独终止；本轮未执行这些命令。

## 结论

- `build/` 当前是 Debug 且 `GALAY_BUILD_BENCHMARKS=OFF`，不能作为 RPC 性能数据来源。
- `build-release/` 当前是 Release 且 `GALAY_BUILD_BENCHMARKS=ON`，RPC benchmark targets 已生成，但没有现成 RPC benchmark 二进制。
- 本轮没有满足“已有 Release benchmark 二进制且无需外部服务即可运行 loopback/本地短测”的条件，因此未运行任何 RPC benchmark。
- ghz、gRPC C++、brpc、thrift 在当前环境不可用，无法产出竞品性能数字。
- 当前 CSV 只记录 blocked/unavailable 状态，不包含伪造吞吐、延迟或资源占用数据。

---

## 2026-07-04 Release unary loopback 结果

**测试环境**: 参考 [测试机器配置](../../../machine_config.md)  
**构建目录**: `build-release` (`CMAKE_BUILD_TYPE=Release`, `GALAY_BUILD_BENCHMARKS=ON`)  
**原始数据**: [rpc_release_loopback_2026-07-04.csv](./benchmark_data/rpc_release_loopback_2026-07-04.csv)

执行命令：

```bash
rtk ../build-release/benchmark/cpp/rpc/benchmark_rpc_unary_loopback_latency 1000
```

| 指标 | 结果 |
|------|------|
| iterations | 1,000 |
| operations | 1,000 |
| errors | 0 |
| elapsed | 0.0447469 s |
| throughput | 22,347.9 ops/s |
| p50 latency | 33 us |
| p90 latency | 70 us |
| p99 latency | 115 us |
| max latency | 231 us |

本轮只覆盖本地 unary echo loopback；同语言 RPC 竞品状态见下一节。

## 2026-07-04 同语言 RPC 竞品状态

**原始数据**: [rpc_same_language_competitor_status_2026-07-04.csv](./benchmark_data/rpc_same_language_competitor_status_2026-07-04.csv)  
**探测输出**: `benchmark/results/rpc/open-source-comparison.txt`

历史计划曾同时列出 gRPC C++、brpc、Apache Thrift C++。按“一模块一个业界标杆”的最新口径，
主排名只保留 gRPC C++；brpc 与 Thrift 的缺失不再阻塞 RPC 交付。

| implementation | 场景 | 状态 | 原因 |
|---|---|---|---|
| gRPC C++ | unary echo | blocked | `grpc_cpp_plugin` 缺失；`grpc` 安装失败；无本地 echo fixture |

因此本轮 RPC 仍因 gRPC C++ 缺失而不能发布主排名结论。
