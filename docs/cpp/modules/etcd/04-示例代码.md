# 04-示例代码

本页只列真实存在的文件、真实存在的 target 和真实可执行命令。

## 1. 运行约定

- 环境变量：当前公开示例 / 测试 / benchmark 都不要求环境变量
- endpoint：除 `T6-EtcdInternalHelpers` 外，统一支持通过 `argv[1]` 传入；未传时回落到 `http://127.0.0.1:2379`
- 推荐命令：始终显式传 `http://127.0.0.1:2379` 或你的企业 endpoint

## 2. examples/：公开示例

| 状态 | 源文件 | target | 覆盖内容 | 运行命令 |
| --- | --- | --- | --- | --- |
| 已验证文件存在 | `examples/etcd/include/e1_basic.cc` | `E1-SyncBasic` | 同步 connect / put / get / del / close | `./build/examples/E1-SyncBasic http://127.0.0.1:2379` |
| 已验证文件存在 | `examples/etcd/include/e2_basic.cc` | `E2-AsyncBasic` | 异步 runtime / scheduler / connect / put / get / del / close | `./build/examples/E2-AsyncBasic http://127.0.0.1:2379` |
| 已验证文件存在 | `examples/cpp/etcd/include/e3_client_pool.cc` | `example_etcd_include_client_pool` | 同步/异步无锁池、租约取得与归还 | `./build-etcd/examples/cpp/etcd/example_etcd_include_client_pool` |
| 条件生成（import） | `examples/etcd/import/e1_basic.cc` | `E1-SyncBasicImport` | `import galay.etcd;` 同步路径 | `./build/examples/E1-SyncBasicImport http://127.0.0.1:2379` |
| 条件生成（import） | `examples/etcd/import/e2_basic.cc` | `E2-AsyncBasicImport` | `import galay.etcd;` 异步路径 | `./build/examples/E2-AsyncBasicImport http://127.0.0.1:2379` |
| 条件生成（import） | `examples/cpp/etcd/import/e3_client_pool.cc` | `example_etcd_import_client_pool` | `import galay.etcd;` client pool 路径 | `./build-etcd/examples/cpp/etcd/example_etcd_import_client_pool` |

说明：

- include/import 文件都是真实样例，不是伪代码
- 这些文件在未传 `argv[1]` 时都会回落到本地 `http://127.0.0.1:2379`；文档命令仍统一显式传参
- import 示例只有在 module/import 编译门控满足且 module facade target 可用时才会生成

## 3. test/：功能验证程序

| 状态 | 源文件 | target | 覆盖内容 | 运行命令 |
| --- | --- | --- | --- | --- |
| 已验证文件存在 | `test/etcd/t1_smoke.cc` | `T1-EtcdSmoke` | 同步 smoke、单 key、lease grant、lease 绑定 | `./build/test/T1-EtcdSmoke http://127.0.0.1:2379` |
| 已验证文件存在 | `test/etcd/t2_prefix.cc` | `T2-EtcdPrefixOps` | 同步前缀 range / prefix delete | `./build/test/T2-EtcdPrefixOps http://127.0.0.1:2379` |
| 已验证文件存在 | `test/etcd/t3_pipe.cc` | `T3-EtcdPipeline` | 同步 pipeline(txn success ops) 编码与结果解析 | `./build/test/T3-EtcdPipeline http://127.0.0.1:2379` |
| 已验证文件存在 | `test/etcd/t4_smoke.cc` | `T4-AsyncEtcdSmoke` | 异步 smoke、lease grant、lease 绑定 | `./build/test/T4-AsyncEtcdSmoke http://127.0.0.1:2379` |
| 已验证文件存在 | `test/etcd/t5_pipe.cc` | `T5-AsyncEtcdPipeline` | 异步 pipeline(txn success ops) | `./build/test/T5-AsyncEtcdPipeline http://127.0.0.1:2379` |
| 已验证文件存在 | `test/etcd/t6_helpers.cc` | `T6-EtcdInternalHelpers` | 纯本地 request builder / response parser / pipeline parser 校验 | `./build/test/T6-EtcdInternalHelpers` |
| 已验证文件存在 | `test/etcd/t7_await.cc` | `T7-EtcdAwaitableSurface` | 公开 awaitable surface 收敛检查 | `./build/test/T7-EtcdAwaitableSurface` |
| 已验证文件存在 | `test/etcd/t8_bench.cc` | `T8-AsyncBenchmarkSmoke` | 复用 benchmark 支撑代码的异步冒烟 | `./build/test/T8-AsyncBenchmarkSmoke` |
| 已验证文件存在 | `test/cpp/etcd/t18_cluster_connection_reuse.cc` | `etcd_t18_cluster_connection_reuse` | 同步/异步池容量、耗尽、RAII 与并发独占 | `./build-etcd/test/cpp/etcd/etcd_t18_cluster_connection_reuse` |

## 4. benchmark/：性能入口

| 状态 | 源文件 | target | 覆盖内容 | 运行命令 |
| --- | --- | --- | --- | --- |
| 已验证文件存在 | `benchmark/etcd/b1_kv.cc` | `B1-EtcdKvBenchmark` | 多线程 put 或 put+get benchmark | `./build/benchmark/B1-EtcdKvBenchmark http://127.0.0.1:2379 8 500 64 put` |
| 已验证文件存在 | `benchmark/etcd/b2_kv.cc` | `B2-AsyncEtcdKvBenchmark` | 异步 put 或 put+get benchmark，可显式指定 I/O scheduler 数量 | `./build/benchmark/B2-AsyncEtcdKvBenchmark http://127.0.0.1:2379 8 500 64 put 2` |
| 已验证文件存在 | `benchmark/cpp/etcd/b6_cluster_connection_reuse.cc` | `benchmark_etcd_b6_cluster_connection_reuse` | 同步/异步池单线程与 8 线程 acquire/release 吞吐 | `./build-etcd/benchmark/cpp/etcd/benchmark_etcd_b6_cluster_connection_reuse 1000000` |

`B1-EtcdKvBenchmark` 的参数定义来自源码：

1. `endpoint`
2. `threads`
3. `ops_per_thread`
4. `value_size`
5. `mode`：`put` 或 `mixed`

`B2-AsyncEtcdKvBenchmark` 的参数定义来自源码：

1. `endpoint`
2. `workers`
3. `ops_per_worker`
4. `value_size`
5. `mode`：`put` 或 `mixed`
6. `io_schedulers`：可选，默认自动推导

## 5. 说明性模式（非 examples/、非 tests/）

以下内容只存在于文档中，不对应 `examples/` 或 `test/` target，因此必须视为“说明性模式”，不是“已验证示例”：

- 服务注册 / 服务发现
- 配置中心
- 简化版分布式锁

这些模式集中放在 [06-高级主题](06-高级主题.md)。如果你要把它们迁入生产代码，建议先：

1. 为模式补充独立 `examples/` 或 `test/`
2. 明确 TLS / 认证 / 条件事务等前置能力是否已经实现
3. 给出真实运行命令和验收标准

## 6. 选择建议

- 想看最短可运行代码：先跑 `E1-SyncBasic` / `E2-AsyncBasic`
- 想验证 prefix：跑 `T2-EtcdPrefixOps`
- 想验证 pipeline：跑 `T3-EtcdPipeline` / `T5-AsyncEtcdPipeline`
- 想做无外部依赖的本地校验：跑 `T6-EtcdInternalHelpers`
