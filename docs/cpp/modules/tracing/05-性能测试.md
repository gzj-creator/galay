# 性能测试

本页记录 `galay-tracing` 自带 benchmark 的用途和运行方式。benchmark 是独立可执行程序，不作为 CTest 通过条件的一部分。

## 2026-07-04 压测状态归档

本次只执行构建与工具状态探测，未启动长时间构建、压测或外部 collector。归档范围对应 `docs/benchmark_plan.md` 的 2.11 `galay-tracing` 场景，以及第 4/7/8 节报告和原始数据归档要求。

**原始状态数据**: [benchmark_data/tracing_benchmark_status_2026-07-04.csv](./benchmark_data/tracing_benchmark_status_2026-07-04.csv)

### 构建与目标状态

| 构建目录 | 构建类型 | `GALAY_BUILD_BENCHMARKS` | tracing benchmark 目标 | tracing benchmark 二进制 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `build/` | `Debug` | `OFF` | 未用于压测 | 未发现 | blocked: 非 Release 且 benchmark 关闭 |
| `../build-release` | `Release` | `ON` | 已生成 `benchmark_tracing_*` 目标 | 未发现 `../build-release/benchmark/cpp/tracing/benchmark_tracing_*` 可执行文件 | blocked: 目标存在但二进制未构建 |

已确认的 `../build-release` tracing benchmark 目标包括：

- `benchmark_tracing_span_scope_throughput`
- `benchmark_tracing_otlp_json_exporter_throughput`
- `benchmark_tracing_batch_processor_schedule_throughput`
- `benchmark_tracing_moodycamel_batch_queue_throughput`
- `benchmark_tracing_disabled_log_throughput`
- `benchmark_tracing_enabled_log_throughput`
- `benchmark_tracing_structured_event_throughput`
- `benchmark_tracing_traceparent_throughput`
- `benchmark_tracing_http_header_integration_throughput`

`../build-release` 的 tracing 模块已开启，`GALAY_TRACING_ENABLE_GALAY_HTTP_OTLP_TRANSPORT=OFF`，`GALAY_TRACING_ENABLE_SPDLOG=OFF`。CMake cache 中 OpenSSL 解析为 `/opt/homebrew/opt/openssl@3`，版本 `3.6.2`。

### 竞品与替代工具状态

| 实现 | 版本 | 构建配置 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| galay | 未重新读取 tag | `../build-release`: Release, benchmark ON | blocked | tracing benchmark 目标存在，但二进制未构建 |
| OpenTelemetry C++ | 未探测 | 未探测 | unavailable | 用户要求停止后未再执行版本探测；没有本次可引用版本 |
| Jaeger client C++ | 未探测 | 未探测 | unavailable | 用户要求停止后未再执行版本探测；没有本次可引用版本 |
| Zipkin client | 未探测 | 未探测 | unavailable | 用户要求停止后未再执行版本探测；没有本次可引用版本 |
| Rust `tracing` 对比 harness | 源码存在，版本未重新读取 | `benchmark/cpp/tracing/compare/rust-tracing` | available-unrun | 可作为本地替代对照，但本次未运行 |

### 2.11 场景状态

| 场景 | galay 目标 | 本次状态 | 原因 |
| --- | --- | --- | --- |
| Span 创建开销 | `benchmark_tracing_span_scope_throughput` | blocked | Release 目标存在，但二进制未构建；按要求未继续构建或运行压测 |
| Exporter 吞吐 | `benchmark_tracing_otlp_json_exporter_throughput` | blocked | `B5` 使用 mock transport，可避免外部 collector；但 Release 二进制未构建 |
| Sampler 开销 | 无独立 sampler 目标；`benchmark_tracing_span_scope_throughput` 仅覆盖 sampled/unsampled span scope | unavailable | 当前目标清单未发现按不同采样策略独立测量的 sampler benchmark |

### 本次已执行的探测命令

```bash
rtk rg -n "^(CMAKE_BUILD_TYPE|GALAY_BUILD_BENCHMARKS|BUILD_BENCHMARKS|BUILD_TESTING):" CMakeCache.txt
rtk rg -n "^(CMAKE_BUILD_TYPE|GALAY_BUILD_BENCHMARKS|BUILD_BENCHMARKS|BUILD_TESTING):" ../build-release/CMakeCache.txt
rtk sed -n '1,120p' ../benchmark/cpp/tracing/CMakeLists.txt
rtk cmake --build ../build-release --target help
rtk proxy find . -maxdepth 5 -type f -perm -111
rtk proxy find ../build-release -maxdepth 5 -type f -perm -111
rtk rg -n "GALAY_BUILD_TRACING|GALAY_ENABLE_OTLP|GALAY_TRACING|OpenSSL|GALAY_BUILD_HTTP" ../build-release/CMakeCache.txt
```

关键输出摘要：

- `build/CMakeCache.txt`: `CMAKE_BUILD_TYPE=Debug`、`GALAY_BUILD_BENCHMARKS=OFF`、`BUILD_TESTING=OFF`。
- `../build-release/CMakeCache.txt`: `CMAKE_BUILD_TYPE=Release`、`GALAY_BUILD_BENCHMARKS=ON`、`BUILD_TESTING=ON`。
- `rtk cmake --build ../build-release --target help` 列出 9 个 `benchmark_tracing_*` 目标。
- `rtk proxy find ../build-release -maxdepth 5 -type f -perm -111` 未列出 tracing benchmark 可执行文件；当前只观察到已构建的 HTTP2 benchmark 可执行文件。

### 补跑命令

仅在允许构建和短测时执行：

```bash
rtk cmake --build ../build-release --target benchmark_tracing_span_scope_throughput benchmark_tracing_otlp_json_exporter_throughput benchmark_tracing_batch_processor_schedule_throughput benchmark_tracing_moodycamel_batch_queue_throughput
rtk ../build-release/benchmark/cpp/tracing/benchmark_tracing_span_scope_throughput
rtk ../build-release/benchmark/cpp/tracing/benchmark_tracing_otlp_json_exporter_throughput
rtk ../build-release/benchmark/cpp/tracing/benchmark_tracing_batch_processor_schedule_throughput
rtk ../build-release/benchmark/cpp/tracing/benchmark_tracing_moodycamel_batch_queue_throughput
```

完整 2.11 对比压测还需要先探测并记录 OpenTelemetry C++、Jaeger client C++、Zipkin client 或替代工具版本，再统一 Release 构建参数、线程数、span 数、batch size 和采样率。

## 构建

```bash
cmake -S . -B build-release -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON -DBUILD_EXAMPLES=ON -DBUILD_BENCHMARKS=ON
cmake --build build-release --parallel
```

## 程序

- `B1-DisabledLog`：禁用日志级别时的热路径开销，预期不写 sink。
- `B2-EnabledLog`：启用日志并写入空 sink 时的格式化与分发开销。
- `B3-SpanScope`：sampled 与 unsampled 子 span 作用域创建和恢复开销。span timestamp 默认关闭、可通过配置开启；同时打印 span/trace ID 生成成本和 subscriber-like noop span scope 作为 Rust `tracing` noop 对照基线。
- `B4-Traceparent`：W3C `traceparent` parse/inject 往返开销。
- `B5-OtlpJsonExporter`：OTLP/HTTP JSON 批量编码到 mock transport 的每 span 开销。
- `B6-StructuredEvent`：结构化事件 disabled 与 enabled no-op writer 热路径开销，覆盖显式 writer、默认 writer、Rust-like callsite 宏路径、默认 writer 宏快路径，以及降级到 `Logger` sink 的 fallback 路径。
- `B7-BatchProcessorSchedule`：对比 `BatchSpanProcessor` 的 `kTimed`、`kOnEnd`、`kBatchSize` 三种后台线程唤醒策略。每种策略运行多轮并打印 best/median 的 `onEnd`、flush 和端到端每 span 开销。
- `B8-MoodycamelBatchQueue`：隔离测试 `moodycamel::ConcurrentQueue` + batch-size 唤醒 + bulk drain 的队列层成本，用于和完整 `BatchSpanProcessor` 成本拆分对照。

每个程序都会打印 workload、build type、backend/adapters 和关键 ns/op 指标，便于后续 release note 引用。

## 历史 Rust 对比（本轮不参与）

按最新要求，本轮竞品对比不使用跨语言样本。本节仅保留历史说明，不作为 2026-07-04 同语言竞品结论。

`benchmark/compare/rust-tracing` 是独立 Cargo 基准，用于在同一机器上对比 Rust `tracing` 的 disabled/enabled no-op/span scope、`serde_json` 生成同形 OTLP JSON 的开销，以及两组 span-like owned payload 批处理基线：

- `R5-RustCrossbeamBatchProcessor`：`crossbeam-channel` bounded queue + batch worker，作为 Rust 开源库完整批处理主对照。
- `R6-RustCrossbeamArrayQueueBatchSize`：`crossbeam-queue::ArrayQueue` + batch-size unpark，作为更贴近 C++ `kBatchSize` 唤醒策略的队列层参考。

运行：

```bash
cargo run --release --manifest-path benchmark/compare/rust-galay-tracing/Cargo.toml
```

做 batch processor 对比时优先比较 `B7-BatchProcessorSchedule mode=batch_size` 和 `R5-RustCrossbeamBatchProcessor`：两者都是 100000 条 span-like owned payload、batch size 512、bounded queue、后台线程批量消费、显式 flush、7 轮 best/median 统计。payload name 固定为 30 字节，避免 C++ `std::string` 小字符串优化和 Rust `String` 指针移动差异影响结论。`B8-MoodycamelBatchQueue` 和 `R6-RustCrossbeamArrayQueueBatchSize` 用于解释队列与唤醒策略本身的成本，不替代完整 processor 对比。Rust `tracing` 的 no-op subscriber 指标只用于框架 callsite 热路径参考，不等价于完整异步 batch processor。

历史 Release 参考结果（不是本次 2026-07-04 压测输出）：

| 项目 | 口径 | median e2e |
| --- | --- | ---: |
| `B7 mode=batch_size` | 完整 `BatchSpanProcessor`，moodycamel queue，按 batch size 唤醒 | 14.23 ns/span |
| `R5-RustCrossbeamBatchProcessor` | `crossbeam-channel` bounded queue + batch worker | 27.54 ns/span |
| `B8-MoodycamelBatchQueue` | 仅 C++ 队列层和 batch-size 唤醒 | 13.72 ns/span |
| `R6-RustCrossbeamArrayQueueBatchSize` | Rust `ArrayQueue` 队列层和 batch-size unpark | 43.65 ns/span |

基准数据受 CPU 频率、系统负载和调度影响，引用时应重新在目标机器运行同一组命令。

---

## 2026-07-04 Release tracing 压测结果

**测试环境**: 参考 [测试机器配置](../../../machine_config.md)  
**构建目录**: `build-release` (`CMAKE_BUILD_TYPE=Release`, `GALAY_BUILD_BENCHMARKS=ON`)  
**原始数据**: [tracing_release_results_2026-07-04.csv](./benchmark_data/tracing_release_results_2026-07-04.csv)

执行命令：

```bash
rtk ../build-release/benchmark/cpp/tracing/benchmark_tracing_span_scope_throughput
rtk ../build-release/benchmark/cpp/tracing/benchmark_tracing_otlp_json_exporter_throughput
rtk ../build-release/benchmark/cpp/tracing/benchmark_tracing_batch_processor_schedule_throughput
```

| 场景 | 指标 | 结果 |
|------|------|------|
| SpanScope | subscriber-like noop | 1.61665 ns/scope |
| SpanScope | span id random | 208.965 ns/op |
| SpanScope | trace id random | 208.106 ns/op |
| SpanScope | sampled span | 189.602 ns/scope |
| SpanScope | unsampled span | 293.969 ns/scope |
| OTLP JSON exporter | ns/span | 122.15 ns/span |
| OTLP JSON exporter | avg body bytes | 1,329 bytes |
| Batch processor timed | median e2e | 49.9658 ns/span |
| Batch processor on_end | median e2e | 15.4654 ns/span |
| Batch processor batch_size | median e2e | 16.2288 ns/span |

本轮不依赖外部 collector，OTLP exporter 使用 mock transport；同语言竞品状态见下一节。

## 2026-07-04 同语言 tracing 竞品状态

**原始数据**: [tracing_same_language_competitor_status_2026-07-04.csv](./benchmark_data/tracing_same_language_competitor_status_2026-07-04.csv)

历史计划曾列出 OpenTelemetry C++、Jaeger client C++、Zipkin client。按最新收敛口径，
主排名只保留 OpenTelemetry C++；Jaeger/Zipkin/Rust tracing 均不再是完成条件。当前执行状态：

| implementation | 状态 | 原因 |
|---|---|---|
| OpenTelemetry C++ | blocked | Homebrew 有 `opentelemetry-cpp` 公式，但依赖 `grpc`；本轮 `grpc` bottle 下载被 reset，无法安装 |
| Jaeger client C++ | unavailable | Homebrew 未发现 `jaeger-client-cpp` 公式 |
| Zipkin client | unavailable | Homebrew 未发现 `zipkin-cpp` 公式 |
| Rust tracing | excluded | 跨语言样本，按最新要求不纳入本轮竞品结论 |

因此本轮 tracing 只因 OpenTelemetry C++ 缺失而不能发布主排名百分比结论。
