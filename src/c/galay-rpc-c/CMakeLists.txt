file(GLOB galay_c_rpc_sources CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/*.cc")
add_library(galay-c-rpc ${galay_c_rpc_sources})
add_library(galay::c-rpc ALIAS galay-c-rpc)
target_compile_features(galay-c-rpc PUBLIC cxx_std_23)
target_include_directories(galay-c-rpc PUBLIC $<BUILD_INTERFACE:${CMAKE_BINARY_DIR}/include> $<INSTALL_INTERFACE:include>)
target_link_libraries(galay-c-rpc PUBLIC galay::c-common)
if(TARGET galay::c-kernel)
    target_link_libraries(galay-c-rpc PUBLIC galay::c-kernel)
endif()
set_target_properties(galay-c-rpc PROPERTIES EXPORT_NAME c-rpc)
