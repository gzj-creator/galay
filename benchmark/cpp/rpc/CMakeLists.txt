function(galay_add_rpc_benchmark target_name source_file)
    add_executable(${target_name} ${source_file})
    target_link_libraries(${target_name} PRIVATE galay::rpc)
    target_include_directories(${target_name} PRIVATE ${CMAKE_BINARY_DIR}/include)
    set_target_properties(${target_name} PROPERTIES CXX_STANDARD 23 CXX_STANDARD_REQUIRED ON)
endfunction()

file(GLOB GALAY_RPC_BENCHMARK_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/b*.cc")
foreach(benchmark_source IN LISTS GALAY_RPC_BENCHMARK_SOURCES)
    get_filename_component(benchmark_name "${benchmark_source}" NAME_WE)
    string(REGEX REPLACE "^b[0-9]+_" "" scenario_name "${benchmark_name}")
    set(target_name "benchmark_rpc_${scenario_name}")

    if(benchmark_name STREQUAL "b12_etcd_managed_client_pressure")
        if(NOT TARGET galay::etcd)
            continue()
        endif()
        if(NOT TARGET galay::rpc-etcd)
            message(FATAL_ERROR "galay::rpc-etcd target is required when rpc and etcd are both enabled")
        endif()
    endif()

    galay_add_rpc_benchmark(${target_name} ${benchmark_source})
    if(benchmark_name STREQUAL "b12_etcd_managed_client_pressure")
        target_link_libraries(${target_name} PRIVATE galay::rpc-etcd)
    endif()
endforeach()
